create function hstore_in(cstring) returns hstore
    immutable
    strict
    parallel safe
    language c
as
$$
begin
-- missing source code
end;
$$;

alter function hstore_in(cstring) owner to postgres;

