create function cube_dim(cube) returns integer
    immutable
    strict
    parallel safe
    language c
as
$$
begin
-- missing source code
end;
$$;

alter function cube_dim(cube) owner to postgres;

