create function ltree_in(cstring) returns ltree
    immutable
    strict
    parallel safe
    language c
as
$$
begin
-- missing source code
end;
$$;

alter function ltree_in(cstring) owner to postgres;

