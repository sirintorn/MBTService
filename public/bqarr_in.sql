create function bqarr_in(cstring) returns query_int
    immutable
    strict
    parallel safe
    language c
as
$$
begin
-- missing source code
end;
$$;

alter function bqarr_in(cstring) owner to postgres;

