create function ltree_out(ltree) returns cstring
    immutable
    strict
    parallel safe
    language c
as
$$
begin
-- missing source code
end;
$$;

alter function ltree_out(ltree) owner to postgres;

