create function icount(integer[]) returns integer
    immutable
    strict
    parallel safe
    language c
as
$$
begin
-- missing source code
end;
$$;

alter function icount(integer[]) owner to postgres;

