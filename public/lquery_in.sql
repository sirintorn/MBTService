create function lquery_in(cstring) returns lquery
    immutable
    strict
    parallel safe
    language c
as
$$
begin
-- missing source code
end;
$$;

alter function lquery_in(cstring) owner to postgres;

